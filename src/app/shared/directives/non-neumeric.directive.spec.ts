import { NonNeumericDirective } from './non-neumeric.directive';

describe('NonNeumericDirective', () => {
  it('should create an instance', () => {
    const directive = new NonNeumericDirective();
    expect(directive).toBeTruthy();
  });
});
